version: "3.9"  # optional since v1.27.0
services:
  web:
    env_file: .env
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    ports:
      - "${PORT}:${PORT}"
    environment:
      - PORT=${PORT}

  dev:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    depends_on:
      - "web"
    volumes:
      - type: bind
        source: ./service
        target: /service
        consistency: cached

